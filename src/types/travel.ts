export type Trip = {
    via_codigo: number;
    via_origem: string;
    via_destino: string;
    via_valor: string;
    via_formapagamento: string;
    via_data: string;
    via_servico: string;
    via_status: string;
    via_observacoes: string;
    mot_modelo: string;
    mot_placa: string;
    fun_nome: string;
    usu_nome: string;
};
